# Contributing

Useful contributions include additional published estimator adapters, tighter correctness bounds, exact arithmetic, SAT/SMT pruning, Pareto-search improvements, reproducible benchmark fixtures, and proof/certificate checking.

Every estimator must document its assumptions and must not present heuristic output as a formal proof.
